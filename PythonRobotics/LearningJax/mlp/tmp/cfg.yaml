meta:
  author: tyx
model:
  inp_dim: 1
  out_dim: 1
train:
  seed: 0
  debug: false
  optimizer: adamw
  lr: 0.001
  grad_norm_clip: 1.0
  batch_size: 128
  max_epoch: 50
checkpoint:
  save_ckpt: true
  ckpt_dir: ./tmp
logging:
  output_dir: ./tmp
